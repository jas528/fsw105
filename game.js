const readlineSync = require ('readline-sync');

const john = readlineSync.question( 'What is your name?');

readlineSync.question ('Hello'+ john + ', Welcome to Doom!  You will risk your life to defeat Bees. Press Enter');

const   = ['Killer Bees', 'Stinger', 'Queen Bee', 'Sting Monster'];
const treasure = ['Comb','Honey', 'Bee Pack', 'Health Postion'];
var prize = [];
let userHealth = 30;
const option = ["Run",'Exit','Hide' ];
let pickUp = treasure[ Math.floor(Math.random() *(5-1+ 2)* 3)]
function game(){
const attactPower = Math.floor(Math.random() *(5-4+3)*5);
const killerBees = killerBees [Math.floor(Math.random() *killerBees.length)];
let badGuysHealth= 58;
const killerBeesPower= Math.floor(Math.random() *(5-3+2)*5)

const index =readlineSync.keyInSelect(options,'What would you like to do next?');

if (options[index]=='Exit'){
return userHealth
} else if (options[index] == " Print" ){
console.log (name + ' : n tresure: '+ pickUp);
} else if (options [index] ==='Walk'){
    let key =Math.random ();
    if (key <= .3) {
        console.log('Walking...');
    } 
     else if (killerBees +' showed up.');

   while(killerBeeshealth > 0 && userHealth > 0) {

    const user = readlineSync.question()
    
switch(user){
    case 'r'://runaway
    const run = Math.random();
    if(run <.5) {
        console.log ('Before you can run and hide' + killerBees + 'sting you with:' + killerBeesPower);
    }else {
        console.log ('Before you can run and hide'+ killerBees + 'attacks you with: '+killerBeesPower);
    }}else {console
        console.log('Run Away!!!');
        break;}}
